# visual-interpreter
I build a lightweight interpreter with integrated execution visualization that shows program behavior step by step during runtime. This makes debugging and learning easier without relying on heavy external debugging tools.
